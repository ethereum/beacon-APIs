get:
  tags:
    - ValidatorRequiredApi
    - Validator
  operationId: "produceInclusionList"
  summary: "Produce an inclusion list"
  description: Requests the beacon node to produce an inclusion list.
  parameters:
    - name: slot
      in: query
      required: true
      description: "The slot for which an inclusion list should be created."
      schema:
        $ref: ../../beacon-node-oapi.yaml#/components/schemas/Uint64
  responses:
    "200":
      description: Success response
      headers:
        Eth-Consensus-Version:
          $ref: "../../beacon-node-oapi.yaml#/components/headers/Eth-Consensus-Version"
      content:
        application/json:
          schema:
            title: ProduceInclusionListResponse
            type: object
            required: [version, data]
            properties:
              version:
                type: string
                enum: [phase0, altair, bellatrix, capella, deneb, electra, focil]
                example: "focil"
              data:
                $ref: "../../beacon-node-oapi.yaml#/components/schemas/Focil.InclusionList"
    "400":
      $ref: "../../beacon-node-oapi.yaml#/components/responses/InvalidRequest"
    "500":
      $ref: "../../beacon-node-oapi.yaml#/components/responses/InternalError"
    "503":
      $ref: "../../beacon-node-oapi.yaml#/components/responses/CurrentlySyncing"

post:
  tags:
    - ValidatorRequiredApi
    - Validator
  operationId: "publishInclusionList"
  summary: "Publish an inclusion list"
  description: "Verifies given inclusion list and publishes it on appropriate gossipsub topic."
  parameters:
    - in: header
      schema:
        $ref: "../../beacon-node-oapi.yaml#/components/schemas/ConsensusVersion"
      required: true
      name: Eth-Consensus-Version
      description: "The active consensus version to which the inclusion list being submitted belongs."
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required: [data]
          properties:
            data:
              $ref: "../../beacon-node-oapi.yaml#/components/schemas/Focil.SignedInclusionList"
  responses:
    "200":
      description: "Successful response"
    "400":
      $ref: "../../beacon-node-oapi.yaml#/components/responses/InvalidRequest"
    "500":
      $ref: "../../beacon-node-oapi.yaml#/components/responses/InternalError"
